package vip.mate.troubleshooting.followup;

/** Deterministic questions supported after a Diagnosis has been created. */
public enum DiagnosisFollowUpIntent {
    WHY,
    EVIDENCE,
    UNKNOWNS,
    NEXT_STEP,
    SUPPLEMENTAL_EVIDENCE,
    HELP,
    END
}
