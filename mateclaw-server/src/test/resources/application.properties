# Surefire calls System.exit after reporting the full suite. Production waits up
# to 30 seconds for scheduled work per application context, but retaining that
# global shutdown hook across hundreds of test contexts serializes those waits
# after all assertions have already completed. TestContext still closes cached
# contexts normally; disabling only the JVM hook keeps test semantics intact.
spring.main.register-shutdown-hook=false
