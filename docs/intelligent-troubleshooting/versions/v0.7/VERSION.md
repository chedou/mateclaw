# v0.7 · 恢复快照

- 状态：历史版本，冻结。
- 恢复来源：2026-07-28 本任务会话中的 v0.7 → v0.8 变更记录。
- 恢复内容：原始 HTML、三份 YAML 源，以及由这些 YAML 重新生成的 Draw.io / SVG。
- 语义：Agent 只看到一个只读证据门面；服务端内部按 Tool Registry 与 EvidenceSourceAdapter SPI 两层插拔。
- 不包含：当时没有单独保存的 RFC / 架构评审逐文件快照；不得把后续 v0.8 内容反向补进本目录。

此目录不得覆盖。任何修订发布为新版本。
